# 写真フォルダの使い方

スタッフIDに対応するファイル名で画像を保存すると、自動的に読み込まれます。

## 対応フォーマット
優先度順に探します：
1. `{ID}.jpg`
2. `{ID}.jpeg`
3. `{ID}.png`
4. `{ID}.webp`
5. `{ID}.gif`

## 例
- スタッフID 1 (ごう) → `1.jpg` または `1.png` など
- スタッフID 5 (けん) → `5.jpg` など

## 注意
- ファイルが存在しない場合は絵文字アイコンが表示されます
- ファイル名は半角数字のみ（スタッフIDと完全一致）
